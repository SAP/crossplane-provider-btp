---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: subaccountdestinations.account.btp.sap.crossplane.io
spec:
  group: account.btp.sap.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - account
    kind: SubaccountDestination
    listKind: SubaccountDestinationList
    plural: subaccountdestinations
    singular: subaccountdestination
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SubaccountDestination manages a destination in a SAP BTP subaccount via
          the Destination Service REST API.

          External-Name Configuration:
            - Follows Standard: no (compound key, not a single GUID)
            - Format: `<subaccount-id>/<destination-name>`
            - How to find:
              - UI: SAP BTP Cockpit → Subaccount → Connectivity → Destinations (field: Name)
              - API: GET /v1/subaccountDestinations/\{destination name\} (fields: subaccount_id + Name)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubaccountDestinationSpec defines the desired state.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: SubaccountDestinationParameters are the configurable
                  fields of a SubaccountDestination.
                properties:
                  additionalConfigurationSecretRefs:
                    description: |-
                      AdditionalConfigurationSecretRefs points to Kubernetes Secrets whose
                      values are JSON objects merged into the destination property bag.
                      All values are coerced to strings (BTP Destination Service expects string-only
                      property values). Use for sensitive properties that must not appear in the CR spec.
                    items:
                      description: A SecretKeySelector is a reference to a secret
                        key in an arbitrary namespace.
                      properties:
                        key:
                          description: The key to select.
                          type: string
                        name:
                          description: Name of the secret.
                          type: string
                        namespace:
                          description: Namespace of the secret.
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    type: array
                  additionalProperties:
                    additionalProperties:
                      type: string
                    description: |-
                      AdditionalProperties are sent as-is to the Destination Service API property bag.
                      Use for all destination-specific fields (e.g. URL, Authentication, ProxyType,
                      TokenServiceURL). For the full list of supported properties see:
                      https://help.sap.com/docs/connectivity/sap-btp-connectivity-cf/destination-service-rest-api-23ccafbea18f4b65919a2799f2cd20e6-150
                      Note: keys "Name" and "Type" override the typed fields above if present.
                    type: object
                  destinationServiceBindingSecretRef:
                    description: |-
                      DestinationServiceBindingSecretRef points to a Kubernetes Secret containing
                      Destination Service OAuth2 credentials. The secret can be created by a
                      ServiceBinding CR (recommended) or manually.

                      Two secret formats are accepted:
                        - Flat keys (leave key empty): the secret has individual keys clientid,
                          clientsecret, tokenurl/token_url, uri/url — the format written by
                          SubaccountServiceBinding when secretKey is not set.
                        - Single JSON key (set key): the named key holds a JSON object with
                          clientid, clientsecret, tokenurl, uri — the format written by
                          SubaccountServiceBinding when secretKey is set (e.g. secretKey: credentials).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  name:
                    description: |-
                      Name of the destination. Immutable after creation.
                      Used as the second segment of the external-name annotation.
                    type: string
                  subaccountId:
                    description: SubaccountID is the GUID of the subaccount that owns
                      this destination.
                    type: string
                  subaccountRef:
                    description: Reference to a Subaccount to populate subaccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  subaccountSelector:
                    description: Selector for a Subaccount to populate subaccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: Type of the destination.
                    enum:
                    - HTTP
                    - RFC
                    - MAIL
                    - LDAP
                    type: string
                required:
                - name
                - type
                type: object
              initProvider:
                description: |-
                  SubaccountDestinationInitParameters holds the reference fields needed by
                  the crossplane reference resolver generator. Mirrors the reference fields
                  in SubaccountDestinationParameters.
                properties:
                  additionalConfigurationSecretRefs:
                    description: |-
                      AdditionalConfigurationSecretRefs points to Kubernetes Secrets whose
                      values are JSON objects merged into the destination property bag.
                      All values are coerced to strings (BTP Destination Service expects string-only
                      property values). Use for sensitive properties that must not appear in the CR spec.
                    items:
                      description: A SecretKeySelector is a reference to a secret
                        key in an arbitrary namespace.
                      properties:
                        key:
                          description: The key to select.
                          type: string
                        name:
                          description: Name of the secret.
                          type: string
                        namespace:
                          description: Namespace of the secret.
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    type: array
                  additionalProperties:
                    additionalProperties:
                      type: string
                    description: |-
                      AdditionalProperties are sent as-is to the Destination Service API property bag.
                      Use for all destination-specific fields (e.g. URL, Authentication, ProxyType,
                      TokenServiceURL). For the full list of supported properties see:
                      https://help.sap.com/docs/connectivity/sap-btp-connectivity-cf/destination-service-rest-api-23ccafbea18f4b65919a2799f2cd20e6-150
                      Note: keys "Name" and "Type" override the typed fields above if present.
                    type: object
                  destinationServiceBindingSecretRef:
                    description: |-
                      DestinationServiceBindingSecretRef points to a Kubernetes Secret containing
                      Destination Service OAuth2 credentials.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  name:
                    description: Name of the destination. Immutable after creation.
                    type: string
                    x-kubernetes-validations:
                    - message: name can't be updated once set
                      rule: self == oldSelf
                  subaccountId:
                    description: SubaccountID is the GUID of the subaccount that owns
                      this destination.
                    type: string
                  subaccountRef:
                    description: Reference to a Subaccount to populate subaccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  subaccountSelector:
                    description: Selector for a Subaccount to populate subaccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: Type of the destination.
                    enum:
                    - HTTP
                    - RFC
                    - MAIL
                    - LDAP
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: SubaccountDestinationStatus defines the observed state.
            properties:
              atProvider:
                description: SubaccountDestinationObservation holds the fields observed
                  from the Destination Service API.
                properties:
                  etag:
                    description: ETag for optimistic concurrency on updates.
                    type: string
                  name:
                    description: Name of the destination as reported by the API.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
