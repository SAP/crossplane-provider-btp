apiVersion: account.btp.sap.crossplane.io/v1beta1
kind: ServiceManager
metadata:
  name: e2e-sm-destination
  namespace: default
spec:
  writeConnectionSecretToRef:
    name: e2e-sm-destination
    namespace: default
  forProvider:
    subaccountRef:
      name: e2e-dest-subaccount
