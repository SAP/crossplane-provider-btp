apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: Entitlement
metadata:
  name: e2e-dest-entitlement
  namespace: default
spec:
  providerConfigRef:
    name: e2e-provider-config
  forProvider:
    serviceName: destination
    servicePlanName: lite
    enable: true
    subaccountRef:
      name: e2e-dest-subaccount
