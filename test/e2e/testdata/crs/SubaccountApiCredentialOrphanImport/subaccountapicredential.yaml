apiVersion: security.btp.sap.crossplane.io/v1alpha1
kind: SubaccountApiCredential
metadata:
  name: sac-orphan-import
spec:
  forProvider:
    readOnly: false
    subaccountRef:
      name: sac-orphan-subaccount
  writeConnectionSecretToRef:
    name: xsuaa-creds-sac-orphan-import
