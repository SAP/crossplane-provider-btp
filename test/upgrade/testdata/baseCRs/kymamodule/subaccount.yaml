apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: Subaccount
metadata:
  namespace: default
  name: upgrade-test-kyma-module-sa
spec:
  forProvider:
    displayName: $BUILD_ID-upgrade-test-kyma-module-sa
    region: eu20
    subdomain: $BUILD_ID-upgrade-test-kyma-module-sa
    labels:
      safe-to-delete: ["yes"]
      BUILD_ID: ["$BUILD_ID"]
    subaccountAdmins:
      - $TECHNICAL_USER_EMAIL
