apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: Entitlement
metadata:
  name: destination-entitlement-upgrade-si
  namespace: default
spec:
  forProvider:
    serviceName: destination
    servicePlanName: lite
    subaccountRef:
      name: upgrade-test-si-sa
