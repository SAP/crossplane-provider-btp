apiVersion: account.btp.sap.crossplane.io/v1beta1
kind: ServiceManager
metadata:
  name: service-manager-upgrade-si
  namespace: default
spec:
  writeConnectionSecretToRef:
    name: service-manager-upgrade-si
    namespace: default
  forProvider:
    subaccountRef:
      name: upgrade-test-si-sa
