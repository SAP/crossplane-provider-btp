apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: ServiceInstance
metadata:
  name: upgrade-test-si
  namespace: default
spec:
  forProvider:
    name: upgrade-test-si
    offeringName: destination
    planName: lite
    parameters:
      HTML5Runtime_enabled: false
    serviceManagerRef:
      name: service-manager-upgrade-si
    subaccountRef:
      name: upgrade-test-si-sa
