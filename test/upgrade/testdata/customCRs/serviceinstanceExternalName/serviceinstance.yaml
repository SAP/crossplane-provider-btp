apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: ServiceInstance
metadata:
  name: upgrade-test-extn-si
  namespace: default
spec:
  forProvider:
    name: upgrade-test-extn-si
    offeringName: destination
    planName: lite
    parameters:
      HTML5Runtime_enabled: false
    serviceManagerRef:
      name: service-manager-si-extn
    subaccountRef:
      name: upgrade-test-extn-si-sa
